[project]
name = "api-ycrousty"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KLOTZ alexandre", email = "klotzalexandre431@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "bcrypt>=5.0.0",
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.5",
    "pydantic-settings>=2.15.0",
    "pyjwt>=2.13.0",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]

[project.scripts]
api-ycrousty = "api_ycrousty:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
